<!DOCTYPE html>
<html lang="en">
    <%-include('./partials/head.ejs') %> 
    <title>Contact</title>
  <body>
      <%-include('./partials/header.ejs') %> 
    <h1>Contact page</h1>

    <div class="contents">
      <% if (blogs.length > 0) { %> <% blogs.map(items => {%>
      <h3><%= items.title%></h3>
      <p><%= items.description%></p>
      <% }) %> <% } %>
    </div>
  </body>
</html>
